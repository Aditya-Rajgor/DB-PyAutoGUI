import pyautogui
import keyboard

# Get the screen resolution
# screenWidth, screenHeight = pyautogui.size()
# print(f"Screen resolution: {screenWidth}x{screenHeight}")

# Get the mouse position
# currentMouseX, currentMouseY = pyautogui.position()
# print(f"Mouse position: {currentMouseX}, {currentMouseY}")

# Move the mouse to the center of the screen
# pyautogui.moveTo(screenWidth / 2, screenHeight / 2, duration=1)

# Move the mouse relative to the current position
# pyautogui.move(100, 100, duration=1)

# Click the mouse at the current position
# pyautogui.click()

# Double-click the mouse at the current position
# pyautogui.doubleClick()

# Right-click the mouse at the current position
# pyautogui.rightClick()

# Middle-click the mouse at the current position
# pyautogui.middleClick()

# Drag the mouse from one position to another
# pyautogui.dragTo(100, 100, duration=1)

# Scroll up
# pyautogui.scroll(100)

# Scroll down
# pyautogui.scroll(-100)

# Take a screenshot
# pyautogui.screenshot("screenshot.png")

# Locate an image on the screen
# image_location = pyautogui.locateOnScreen("image.png")
# print(image_location)

# Get the center of the located image
# center_x, center_y = pyautogui.center(image_location)
# print(f"Center of the image: {center_x}, {center_y}")

# Click on the center of the located image
# pyautogui.click(center_x, center_y)

# Find all occurrences of an image on the screen
# image_locations = list(pyautogui.locateAllOnScreen("image.png"))
# print(image_locations)

# Get the number of occurrences of an image on the screen
# num_occurrences = len(image_locations)

# Get the position of the first occurrence of an image
# first_occurrence = image_locations[0]

# Get the confidence of the first occurrence
# confidence = first_occurrence[2]

# Get the region of the first occurrence
# region = first_occurrence[0]

# Get the size of the first occurrence
# width, height = first_occurrence[1]

# Get the left, top, right, and bottom coordinates of the first occurrence
# left, top, right, bottom = region

# Get the x and y coordinates of the center of the first occurrence
# center_x = left + (width / 2)

# center_y = top + (height / 2)

# Click on the center of the first occurrence
# pyautogui.click(center_x, center_y)

# Get the position of the first occurrence of an image
# first_occurrence = pyautogui.locateCenterOnScreen("image.png")

# Click on a search box
# pyautogui.click(1017, 28)

# Type 'Hello, World!' into the search box
# pyautogui.typewrite("image.png")

# Press the Enter key
# pyautogui.("enter")
# keyboard.press_and_release("enter")

# Press the Tab key

